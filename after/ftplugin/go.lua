-- Go uses tabs; everything else matches the global defaults.
-- Format-on-save (goimports + gofmt) is handled by plugins/conform.lua.
vim.opt_local.expandtab = false
vim.opt_local.commentstring = '//%s'
