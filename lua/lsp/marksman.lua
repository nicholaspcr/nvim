-- Markdown. No overrides: nvim-lspconfig's lsp/marksman.lua already supplies
-- the same cmd and filetypes, and single_file_support no longer exists in the
-- 0.11+ vim.lsp.config world. Listed so mason installs the server.
return {}
